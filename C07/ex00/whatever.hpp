#ifndef __WEVER__
#define __WEVER__

template<typename T>
void	swap(T& first, T& other);

template<typename T>
T	min(T& first, T& other);

template<typename T>
T	max(T& first, T& other);

#endif // __WEVER__